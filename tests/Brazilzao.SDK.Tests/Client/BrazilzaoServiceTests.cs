using NUnit.Framework;

namespace Brazilzao.SDK.Tests.Client
{
    [TestFixture]
    public class BrazilzaoServiceTests
    {
        [Test]
        public void Test()
        {
            //var service = new BrazilzaoService();

            //var dateInputModel = new DateInputModel() { ChampionshipID = 1, Day = 1, Month = 1, Year = 1 };

            //var result = service.GetClassificationByDate(dateInputModel).GetAwaiter().GetResult();
        }
    }
}
